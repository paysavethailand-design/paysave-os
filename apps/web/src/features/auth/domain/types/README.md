# Authentication Domain Types

Reserved for authentication-owned domain types. Shared identity and permission primitives remain in `@paysave/security`; cross-boundary DTOs belong in `@paysave/contracts`.
