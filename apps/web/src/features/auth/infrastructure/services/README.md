# Authentication Infrastructure Services

Reserved for implementations of authentication application ports. Supabase-specific adapters remain owned by this feature and are not imported directly outside its public server entrypoint.
