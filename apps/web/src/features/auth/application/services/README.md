# Authentication Application Services

Reserved for orchestration contracts owned by authentication. It may depend on domain abstractions but must not import Supabase, Next.js, React, or infrastructure implementations.
