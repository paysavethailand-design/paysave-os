# RFC-0008 — Risk Register

- **Status:** FINAL — SEALED FOR CTO DECISION; RISKS REMAIN OPEN
- **Scale:** Critical / High / Medium / Low

| ID    | Risk                                                                   | Severity | Evidence/trigger                                                                       | Mitigation                                                                                                       | Owner                   | Gate impact                    |
| ----- | ---------------------------------------------------------------------- | -------- | -------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- | ----------------------- | ------------------------------ |
| R8-01 | `tenant_scope=all` bypasses membership validation in frozen RLS        | Critical | M003 `admin.is_global_admin()` trusts the claim                                        | Disable issuance until global authority and emergency revocation are approved; five-minute token TTL             | CISO / IAM Lead         | Blocks global admin and Beta   |
| R8-02 | No authoritative global-admin grant exists in approved IAM contract    | Critical | Partner-scoped memberships/roles only; no frozen global grant source                   | CTO selects governed source or defers global scope                                                               | CTO / Architecture      | Blocks final claim resolver    |
| R8-03 | No persisted active-partner selector exists                            | High     | `iam.users` has no `active_partner_id`                                                 | Server-controlled Auth/session selector; auto-select only one unambiguous membership; always revalidate          | IAM Lead                | Blocks multi-partner login     |
| R8-04 | Service-role credential is broader than read-only resolver need        | Critical | No approved least-privilege hook credential path                                       | Secret isolation, restricted resolver module, egress controls, audit, key rotation; Security acceptance required | Security Platform       | Blocks implementation          |
| R8-05 | Permission claims become stale after revoke                            | High     | JWT snapshot remains valid until `exp`                                                 | Five-minute TTL, revoke sessions, fresh checks for high-risk actions                                             | IAM / App Security      | Blocks Beta until measured     |
| R8-06 | Global token remains valid after membership/user changes               | Critical | Global RLS path does not check membership/user status                                  | Keep `all` disabled; later add approved revocation architecture only through separate decision                   | CISO                    | Blocks global scope            |
| R8-07 | Branch scope is not enforced by generic RLS                            | High     | Branch scope table exists but domain policies use partner authorization only           | Route branch-sensitive traffic through server fresh-scope resolver; prohibit direct Data API path                | Domain Leads            | Blocks direct browser access   |
| R8-08 | Permission/resource scope is not enforced by generic RLS               | High     | Permission-scope rows are not consumed by domain RLS                                   | Fresh server resolution and deny by default                                                                      | IAM / Domain Leads      | Blocks sensitive workflows     |
| R8-09 | Role code contract mismatch                                            | High     | IAM role code is free-form; parser accepts five enum values                            | Approve vocabulary; unknown roles deny issuance; no silent drop                                                  | IAM Governance          | Blocks custom roles            |
| R8-10 | Role-permission effect semantics are undefined                         | High     | `effect` is non-empty text without frozen enum                                         | Approve allow/deny vocabulary and deny-overrides rule; unknown values deny                                       | CISO / IAM              | Blocks permission emission     |
| R8-11 | `session_version` gives false revocation confidence                    | High     | Current parser requires it; approved schema has no source/comparison                   | Mark compatibility-only constant; never cite as revocation evidence                                              | App Security            | Blocks any version-based claim |
| R8-12 | Oversized JWT/cookies break SSR or expose excessive authorization data | High     | Parser permits up to 200 permissions                                                   | 2 KiB custom/4 KiB token budgets; reject, never truncate; measure cookie envelope                                | Web Platform            | Blocks Beta if exceeded        |
| R8-13 | Hook outage blocks login and refresh                                   | High     | Hook is on token-issuance critical path                                                | Multi-zone managed runtime, bounded timeout, health/alerting; fail closed                                        | Platform/SRE            | Blocks Beta operational gate   |
| R8-14 | Hook webhook replay or spoofing                                        | Critical | Public HTTP integration endpoint                                                       | Standard Webhooks signature, timestamp tolerance, replay cache, endpoint secret rotation                         | Security Platform       | Blocks implementation          |
| R8-15 | Auth cache serves revoked grants                                       | High     | Positive cache may outlive change                                                      | 30–60 second max TTL, invalidation event, session revoke; deny on ambiguity                                      | IAM Platform            | Blocks Beta until drill        |
| R8-16 | Disabled IAM user may still pass partner RLS                           | High     | Frozen partner authorization checks membership but not user status                     | Disable Auth identity, revoke sessions and deactivate memberships atomically at workflow level                   | IAM Operations          | Blocks disable-user acceptance |
| R8-17 | Stale legacy setup instructions cause prohibited migration use         | Critical | `AUTHENTICATION_SETUP.md` references legacy 0001/0002                                  | Mark superseded in a separate approved doc-only change; CI prohibited-path check                                 | Architecture Governance | Blocks operator handoff        |
| R8-18 | Sensitive claim/log leakage                                            | High     | Tokens and permission arrays may enter logs/traces                                     | Never log tokens/claims; log counts/version/correlation only; redact headers/cookies                             | SRE / Security          | Blocks production readiness    |
| R8-19 | Direct Data API path bypasses app permission/branch checks             | Critical | Frozen RLS enforces partner, not all fine-grained policy                               | Endpoint/data-path allowlist; application-only routing for sensitive resources                                   | App Security            | Blocks Beta scope approval     |
| R8-20 | Claim resolver semantics drift from IAM governance                     | High     | Status/effect/time-window semantics are not fully enumerated                           | Versioned resolver contract, golden tests, owner sign-off and audit evidence                                     | IAM Lead                | Blocks implementation          |
| R8-21 | Target `claims_version` is not enforced by the current parser          | High     | Current `AuthContext` parser does not inspect the target field                         | Separately authorize parser contract change and negative version tests; no conformance claim before evidence     | App Security            | Blocks implementation          |
| R8-22 | HTTP hook binding and fail-closed runtime behavior are unproven        | Critical | No approved Edge Function, Auth hook config, issued-token proof or outage drill exists | Require deterministic binding evidence, signed-event tests, issued-token acceptance and outage/rollback test     | Security Platform / SRE | Blocks implementation and Beta |
| R8-23 | Service-role use in a user-context path could bypass RLS               | Critical | Service role is broad and frozen RLS is bypassed by that capability                    | Prohibit user-path use; isolate resolver access, assert role, audit every use and obtain Security approval       | App Security / Platform | Blocks implementation          |

## Residual-risk rule

No Critical risk is waivable for Beta when it can produce cross-tenant access, global-authority persistence, credential exposure or bypass of a fine-grained control required by the business workflow.
