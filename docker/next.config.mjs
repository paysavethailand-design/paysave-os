/** Runtime equivalent of apps/web/next.config.ts; JavaScript avoids runtime TypeScript installation. */
const nextConfig = {
  poweredByHeader: false,
  reactStrictMode: true,
  typedRoutes: true,
};

export default nextConfig;
