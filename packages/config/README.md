# @paysave/config (Reserved)

Boundary สำหรับ shareable TypeScript, ESLint และ formatting presets เมื่อมี app/package มากกว่าหนึ่งชุดที่ต้องใช้จริง

Stage 1 ใช้ root config ปัจจุบันก่อนตาม YAGNI และจะเปิด package นี้หลังอนุมัติ
