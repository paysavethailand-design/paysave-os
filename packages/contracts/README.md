# @paysave/contracts (Reserved)

Boundary สำหรับ versioned DTO และ event schema ที่ใช้ข้าม Feature/Process

ยังไม่เปิดเป็น workspace packageจนกว่าจะมี contract ที่ได้รับอนุมัติ; ห้ามใส่ repository หรือ UI implementation
